export declare class Container<T> implements Iterable<T> {
    private _parent?;
    private _values;
    constructor(_parent?: Container<T> | undefined);
    [Symbol.iterator](): IterableIterator<T>;
    push(...items: T[]): void;
    readonly length: number;
}
